# Threat Model

**Status:** DESIGNED

Identified threats include path traversal, zip bombs (large excel files), and unauthorized job access. Mitigation layers include FastAPI limits, file sanitization, and upcoming RBAC.
