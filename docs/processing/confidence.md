# Mapping Confidence

**Status:** IMPLEMENTED

The intelligence service uses fuzzy matching (RapidFuzz) to detect headers, assigning a confidence score (0.0 to 1.0). Confidence is distinct from Row Routing.
